spring:
  boot:
    admin:
      client:
        url: http://localhost:1111 


  datasource:
    username: root
    password: Amruta@123
    url: jdbc:mysql://localhost:3306/sbms38
  

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8083
